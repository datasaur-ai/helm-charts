{{- if .Values.services.audiowaveformGenerator.enabled -}}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-audiowaveform-generator-service
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-audiowaveform-generator
  ports:
    - protocol: TCP
      port: 5005
      targetPort: 5005
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ .Release.Name }}-audiowaveform-generator
  name: {{ .Release.Name }}-audiowaveform-generator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-audiowaveform-generator
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-audiowaveform-generator
    spec:
{{ include "imagePullSecretTemplate" . | nindent 6 }}
      containers:
{{- if eq (printf "%.31s" .Values.image.registry) "harbor-test.liberty.datasaur.ai" }}
        - image: harbor-test.liberty.datasaur.ai/shared/vulcan-ai/vulcan/audiowaveform-generator:{{ .Values.image.dockerTag }}
{{- else }}
        - image: {{ .Values.image.registry | trimSuffix "/" }}{{ if .Values.image.registry }}/{{ end }}vulcan-ai/vulcan/audiowaveform-generator:{{ .Values.image.dockerTag }}
{{- end }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          name: {{ .Release.Name }}-audiowaveform-generator
          envFrom:
            - configMapRef:
                name: datasaur-env
            - secretRef:
                name: datasaur-secret
{{- end -}}
